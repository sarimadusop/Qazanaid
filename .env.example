# Kazana Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=production

# Session Secret (Change this!)
SESSION_SECRET=your_super_secret_session_key_here

# Local Database Configuration (VPS)
# Used by the application within Docker
DATABASE_URL=postgresql://postgres:password@db:5432/Qazanaid

# Local PostgreSQL Details (used by docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=Qazanaid

# Legacy Database (Only for migration reference)
# SUPABASE_DATABASE_URL=your_old_supabase_url
